# Style Settings Colors

<!-- Record the customer's original Style Settings colors from KnowledgeOwl
     (Customize > Style > Style Settings > Colors). The color picker only reveals
     one hex code at a time, so click each swatch to reveal its hex and fill it in
     below. Leave a value blank if the customer has no value set for that swatch.

     KnowledgeOwl uses two different labeling schemes for these swatches depending
     on the KB's theme. Each row below lists both wordings ("common / variant") —
     match whichever your KB shows; the swatch order is identical in both. Delete
     the wording you don't need (and this comment) once the values are filled in. -->

- **Top navigation bar** / **Header background:** `#`
- **Top navigation text** / **Header text:** `#`
- **H1s, H2s, H3s, etc.** / **Header tags:** `#`
- **Table of contents** / **Column background:** `#`
- **Table of contents text** / **Column text:** `#`
- **Highlights & Accents** / **Highlights & accents:** `#`
- **Default category icon colors — Icon color:** `#`
- **Default category icon colors — Icon background:** `#`
