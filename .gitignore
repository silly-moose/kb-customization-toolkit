# macOS system files
.DS_Store

# Thumbnail cache
Thumbs.db

# Localhost preview (ephemeral, generated per session)
preview/

# Private, local-only improvement log (never publish — contains internal/customer notes)
improvement-log.md

# Per-user Claude Code local settings (never ship — teammates have their own)
**/.claude/settings.local.json

# kb-io scratch (live reads, plans, call files) and Python bytecode
process-docs/kb-io/work/
__pycache__/
